# 项目开发规则

## 业务边界

1. 本项目不使用 LLM 或生成式 AI 进行任何业务判断。
2. OCR 仅用于文字与坐标识别，以及姓名、学号、日期、机构名称等候选位置辅助。
3. 评分规则必须是确定性的、可测试的、可解释的并可追溯到规则版本。
4. 证明材料的最终有效性必须由人工审核。
5. 身份定位优先使用学号精确匹配，再使用姓名匹配；模糊匹配只能作为候选提示。
6. OCR 失败、低置信度或无匹配时，必须允许用户选择候选或人工框选，不得阻断申报。

## 权限与隐私

7. 学生只能访问自己的材料、OCR 结果和申报。
8. 班委只能管理其被授权班级的数据。
9. 不得提交真实学生姓名、学号、成绩、证明材料或原始班级汇总表。
10. 不得提交密码、Token、私钥或真实环境变量。演示数据只能使用明确虚构的信息。

## 工程约束

11. 数据库 schema 变化统一使用 Flyway migration，不得手工修改共享数据库结构。
12. 非平凡修改前优先使用 `explorer` 调查调用链和影响范围。
13. 生产代码主要由主 agent 修改；`tester` 默认只修改测试代码。
14. 完成实现后优先调用 `tester` 验证，再调用 `reviewer` 审查。
15. 避免多个写入型 agent 并行修改同一模块。
16. 未经明确要求，不进行大范围重构，不增加 Kubernetes、Redis、消息队列、Elasticsearch、MinIO 或 LLM API。

默认工作流：`explorer` 调查 → 主 agent 实现 → `tester` 测试 → `reviewer` 审查。
